name: Publish Docs

on:
  push:
    branches: [main]

jobs:
  build-deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: quarto-dev/quarto-actions/setup@v2
      - run: quarto render documentation/
      - uses: quarto-dev/quarto-actions/publish@v2
        with:
          target: gh-pages
          path: documentation/
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}